#include <iostream>
#include <string>
#include <cstring>
#include <vector>
#include <stack>
#include <cmath>

int getInputs(std::vector<std::string> &);
void convertInputs(std::vector<std::string> &, int);
void convert(std::string);
void solveEqu(std::string);
int doMaths(int, int, char);